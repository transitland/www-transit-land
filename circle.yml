machine:
  ruby:
    version: 2.1.2
database:
  override:
    - echo "we need no database" 
test:
  override:
    - echo "we need no tests"
dependencies:
  post:
    - bundle exec jekyll build

deployment:
  staging:
    branch: master
    commands:
      - TARGET=staging bundle exec s3_website push
  production:
    branch: production 
    commands:
      - TARGET=prod bundle exec s3_website push
